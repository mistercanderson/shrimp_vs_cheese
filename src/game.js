class Game {

  constructor(height, width) {
    this.height = height;
    this.width = width;
  };

  displayBoard() {
    var board = [];
    var space = [];
    var boardArea = this.height * this.width;
    for (var i = 0; i < area; i++) {
      board.push(space);
    };
  };

};
